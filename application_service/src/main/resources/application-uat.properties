# UAT profile - staging/pre-release environment (isolated database, verbose logging)
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:jobapp_uat_db}
logging.level.org.omnaphade=DEBUG
